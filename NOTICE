pg_textsearch_ko — Third-Party Notices

This extension family is licensed under the PostgreSQL License (see LICENSE).
It builds on and/or depends on the following third-party components.

[Embedded — Rust crates compiled into pg_tsvector_ko / pg_textsearch_ko]
  lindera            — MIT OR Apache-2.0. Korean/CJK morphological analysis engine
                       (forked from kuromoji-rs). https://github.com/lindera/lindera
  lindera-ko-dic     — Apache License 2.0. ko-dic dictionary data, derived from
                       mecab-ko-dic (eunjeon / 은전한닢) and the 21st Century Sejong
                       Project corpus. Embedded via the `embed-ko-dic` feature.
  (+ all other statically-linked crates: pgrx, serde, etc. — permissive MIT/Apache/
   BSD/ISC. The complete, authoritative list with full license/copyright texts is in
   THIRD_PARTY_LICENSES/RUST_CRATES.html, generated from Cargo.lock by cargo-about and
   gated by cargo-deny — see THIRD_PARTY_LICENSES/README.md.)

[Runtime dependencies — depended on via `requires`]
  pg_textsearch (Timescale/Tiger Data) — PostgreSQL License. BM25 index (Layer B).
  pgvector                             — PostgreSQL License. Dense vector search.

  Distribution mode matters:
    - Source / `make install-all`: these are NOT redistributed; they are fetched
      from their official repositories at build time and installed separately.
    - Docker image (`make docker-build`): these ARE bundled as compiled binaries
      together with PostgreSQL. Their license texts are reproduced under
      THIRD_PARTY_LICENSES/ to satisfy attribution. `pg_textsearch` is a trademark
      of Timescale/Tiger Data; this bundle is not an official Timescale product.

[Optional, opt-in feature — NOT enabled by default]
  Kiwi (libkiwi)     — LGPL 2.1+. Alternative Korean analyzer, dynamically linked
                       only when the `kiwi` feature is enabled. Not in default
                       builds or the default image. (follow-on)

See THIRD_PARTY_LICENSES/ for bundled license texts and docs/DESIGN.md (§6 license
audit) for the full analysis.
